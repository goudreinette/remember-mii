{
    "build": {
        // Path is relative to the devcontainer.json file.
        "dockerfile": "Dockerfile"
    },

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-vscode.cpptools-extension-pack",
				"Tyriar.luna-paint"
				// "softpack6ix.nds-emulator-vscode"
			]
		}
	},
	"postCreateCommand": "git submodule update --init --recursive && cd GRRLIB && make -C GRRLIB clean all install"
}